# Happbit Web

## Requisitos
- Node 22.x
- pnpm

## Configuración
Crear `apps/web/.env`:

VITE_API_URL=http://localhost:3001

## Correr
Desde el root del monorepo:

pnpm -C apps/web install
pnpm -C apps/web dev
